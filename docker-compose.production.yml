version: '3'

volumes:
  pg_data:
  static:

services:
  db:
    image: postgres:13
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data

  backend:
    image: hourvari/kittygram_backend
    env_file: .env
    volumes:
      - static:/static
    depends_on:
      - db

  frontend:
    image: hourvari/kittygram_frontend
    env_file: .env
    command: cp -r /app/build/. /static/
    volumes:
      - static:/static

  gateway:
    image: hourvari/kittygram_gateway
    env_file: .env
    ports:
      - 9000:80
    volumes:
      - static:/static
    depends_on:
      - backend
      - frontend
